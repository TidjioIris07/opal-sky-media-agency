enum UserRole {
  SUPER_ADMIN
  EDITOR
}

model User {
  id        String   @id @default(uuid())
  email     String   @unique
  password  String
  role      UserRole @default(EDITOR)
  createdAt TimestamptzString @default(now())
}